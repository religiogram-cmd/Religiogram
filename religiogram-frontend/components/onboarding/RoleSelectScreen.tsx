/**
 * RoleSelectScreen — retired.
 *
 * Sign-up no longer asks the user to pick a role. Every account starts as
 * a standard user; the multi-step profile setup at /profile-setup collects
 * the remaining details.
 *
 * Component kept as an empty stub so accidental imports don't break the
 * build. Safe to delete once we're sure no consumer references it.
 */
export default function RoleSelectScreen() {
  return null;
}

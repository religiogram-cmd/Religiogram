/**
 * BecomePriestScreen — disabled in this build.
 *
 * Provider/priest features are paused while we focus on the standard
 * single-user flow. The component is retained as a placeholder so existing
 * imports compile, but it renders nothing meaningful and is no longer
 * routed from anywhere.
 *
 * When the priest flow returns, restore from git history.
 */
export default function BecomePriestScreen() {
  return null;
}
